<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Links</title>
</head>

<body>
     <h1>Links</h1>
     <a href="/addlink">Adicionar Link</a>
    <% for( let i = 0; i < links.length; i++ ) { %>
    <div id="<%= links[i]._id %> ">
        <form method="POST" action="/?_method=DELETE">
            <h1>ID: <%= links[i]._id %> </h1>
            <h2><%= links[i].title %> </h2>
            <div><%= links[i].description %> </div>
            <div><%= links[i].url %> </div>
            <input type="hidden" name="id" value="<%=links[i]._id%>">
            <button type="submit">Delete</button>
            <a href="/editlink/<%=links[i]._id%>">EDIT</a>
        </form>
    </div>
    <% } %>
</body>

</html>