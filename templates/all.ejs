<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Links</title>
</head>

<body>
    <%- include('partials/style.ejs') %>

    <div>
        <h1 class="title-body">Links</h1>
        <a class="add-links" href="/addlink">Adicionar Link</a>
    </div>
    <% for( let i = 0; i < links.length; i++ ) { %>
    <div class="card-box" id="<%= links[i]._id %> ">
        <form method="POST" action="/?_method=DELETE">
            <h2 class="title-card"><%= links[i].title %> </h2>
            <h3 class="title-body-card"><%= links[i].description %> </h3>
            <a class="my-link" href="<%= links[i].url %> ">My Link</a>
            <input type="hidden" name="id" value="<%=links[i]._id%>">
            <a class="btn-edit" href="/editlink/<%=links[i]._id%>">EDIT</a>
            <button class="btn-card" type="submit">Delete</button>
        </form>
    </div>
    <% } %>
</body>

</html>