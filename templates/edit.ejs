<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>

<body>
    <%-include("partials/style.ejs")%> 
    <div>
        <h1 class="title-body">EDIT LINK</h1>
        <div class="container-form">
        <form  action="/edit/<%=body.id%>" method="POST">
            <input class="field" name="title" placeholder="title" value="<%=body.title%>" required>
            <input class="field" name="description" placeholder="Description" value="<%=body.description%>">
            <input class="field" name="url" placeholder="URL" value="<%=body.url%>" required>
            <button class="edit">EDIT</button>
        </form>
        </div>
    </div>

    <%if (error) { %>
    <h3> <%= error.message %> </h3>
    <% } %>

</body>

</html>